#!/usr/bin/env bash

# Kill chromedriver process
stop-chrome-headless() {
    echo "Stop Chrome process with PID:" $(cat ~/chromedriver.pid)
    kill -9 $(cat ~/chromedriver.pid) > /dev/null 2>&1 && rm -f ~/chromedriver.pid
}

# Stop
stop-chrome-headless